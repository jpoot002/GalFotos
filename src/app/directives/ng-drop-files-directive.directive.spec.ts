import { NgDropFilesDirectiveDirective } from './ng-drop-files-directive.directive';

describe('NgDropFilesDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDropFilesDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
